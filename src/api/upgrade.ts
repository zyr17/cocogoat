export async function latestRelease(current: string = '', mode: string = 'auto') {
    let fileName
    let githubUrl
    return {
        version: '0.5.0',
        name: '',
        file: fileName,
        content: '',
        url: {
        },
        _raw: '',
    }
}
export async function checkPatch(url: any, hash: string) {
    const errmsg = { message: 'NotImplemented' };
    throw errmsg;
}
